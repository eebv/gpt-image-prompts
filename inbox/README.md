# Inbox

把待导入的图片和提示词放到这里。

## 推荐规则

使用**同名配对**：

- `001.png`
- `001.txt`

- `002.webp`
- `002.md`

脚本会自动把同名图片和提示词配对导入。

## 当前默认分类

- 用途：`头像`
- 风格：`电影感`

后续如果需要，可以再扩成按文件名或 sidecar 配置自动指定分类。

## 导入命令

```bash
npm run import:inbox
```

或双击：

- `run-import-inbox.cmd`
